import { EmptyState } from '../../components/EmptyState'

export function StudentsPage() {
  return (
    <EmptyState
      title="Students are next"
      description="Student profiles will bring together school scope, guardian links, roles, and class membership."
    />
  )
}