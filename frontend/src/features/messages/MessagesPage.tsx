import { EmptyState } from '../../components/EmptyState'

export function MessagesPage() {
  return (
    <EmptyState
      title="Messages are next"
      description="School communication will support role-aware updates between staff, students, and families."
    />
  )
}